name: ThinkStats
channels:
  - conda-forge
dependencies:
  - pytest
  - nbclassic
  - jupytext
  - nbmake
  - black
  - flake8
  - pip
  # Jupyter Book (jb/build.sh) — jb/ config is for the 1.x CLI, not 2.x
  - "jupyter-book<2"
  - ghp-import
  - pip:
    - nbformat

